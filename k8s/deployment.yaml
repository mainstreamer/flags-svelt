apiVersion: apps/v1
kind: Deployment
metadata:
  name: capitals-ui
  namespace: capitals-ui
  labels:
    app: capitals-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: capitals-ui
  template:
    metadata:
      labels:
        app: capitals-ui
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: capitals-ui
          image: ghcr.io/mainstreamer/capitals-ui:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
